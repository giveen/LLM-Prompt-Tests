# Core dependencies for Security Benchmark Tool
# Required for eval2.py

# Environment variable management
python-dotenv>=1.0.0

# HTTP requests for model pricing
requests>=2.31.0

# LLM interface library
litellm>=1.0.0

# Optional but recommended for better terminal output
rich>=13.0.0

# For data processing (included in standard library, but good to note)
# json, re, os, sys, csv, argparse, datetime are built-in Python modules
